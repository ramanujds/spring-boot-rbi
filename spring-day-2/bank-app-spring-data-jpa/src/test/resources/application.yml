
account:
  min_balance: 1000


spring:
  profiles:
    active: ${ACTIVE_PROFILE:}
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    properties:
        hbm2ddl:
          auto: update

server:
  port: 5000